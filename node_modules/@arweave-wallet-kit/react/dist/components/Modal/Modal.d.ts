import { PropsWithChildren } from "react";
import { MotionProps } from "framer-motion";
export declare function Modal({ open, onClose, children, className, variants, noWatermark, noVariableContentSize }: PropsWithChildren<Props>): import("react/jsx-runtime").JSX.Element;
interface Props extends MotionProps {
    open: boolean;
    onClose: () => void;
    className?: string;
    noWatermark?: boolean;
    noVariableContentSize?: boolean;
}
export {};
